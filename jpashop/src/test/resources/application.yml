# 별도의 설정이 없어도 기본적으로 메모리 모드로 돌려버린다!!!!

# spring:
# datasource: #띄어쓰기 2칸
#   url: jdbc:h2:mem:test #4칸
#   username: sa
#   password:
#   driver-class-name: org.h2.Driver

# jpa:
#   hibernate:
#     ddl-auto: create
#   open-in-view: false
#   properties:
#     hibernate:
#       '[show_sql]': true
#       '[format_sql]': true

# output:
#   ansi:
#     enabled: always

logging:
  level:
    '[org.hibernate.SQL]': debug
    '[org.hibernate.type]': trace
